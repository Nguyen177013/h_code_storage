type datasetType = {
    label: string,
    data: number[]
    borderColor: string,
    backgroundColor: string,
}

type datetimeSelectionType = {
    value: string,
    label: string
};

type dateOptionType = {
    year:string
    option:string
}

