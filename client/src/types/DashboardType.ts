type datasetType = {
    label: string,
    data: number[]
    borderColor: string,
    backgroundColor: string,
    pointRadius: number,
    pointHoverRadius: number,
    pointHitRadius: number, 
}

type datetimeSelectionType = {
    value: string,
    label: string
};

type dateOptionType = {
    year:string
    option:string
}

