type SauceHistory = {
    dateFormat: number,
    total: number
}

type SauceHistoryProp = {
    "dateFormat": string[],
    "total": number[],
    "filterBy":string
}

type TotalUpload ={
    "totalUpload": number
}
